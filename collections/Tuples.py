# Tuples:Collection values
t = (1, 2, "3", 4)

# Accessing the value
print(t[0])
print(t[-1])

# Count & index
t = ('a', 'a', 'b')
print(f"Number of occurances of a in t is {t.count('a')} and its index is {t.index('a')}")
